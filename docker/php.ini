; Sulu's cache warmup and admin UI need more than the 128M PHP default.
; Image processing runs through GD, which decodes images fully into the
; PHP heap — large uploads need generous headroom here.
memory_limit = 4096M

; Media uploads through the Sulu admin.
upload_max_filesize = 128M
post_max_size = 128M
