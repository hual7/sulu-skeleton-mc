; Sulu's cache warmup and admin UI need more than the 128M PHP default.
; Image processing uses Imagick (memory outside the PHP limit), but keep
; headroom for the GD fallback with large uploads.
memory_limit = 4096M

; Media uploads through the Sulu admin.
upload_max_filesize = 128M
post_max_size = 128M
